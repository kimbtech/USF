language: java
branches:
  only:
    - "master"
script: mvn javadoc:javadoc && mvn test
deploy:
  - provider: pages
    github-token: $GITHUB_TOKEN
    local-dir: "target/site/apidocs/"
    skip-cleanup: true
    on:
      branch: master
after_success: mvn jacoco:report coveralls:jacoco
